spring:
  datasource:
    url: jdbc:h2:mem:wallet;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  rabbitmq:
    listener:
      simple:
        auto-startup: false

cafeorbe:
  wallet:
    saldo-inicial: 1000
